program myshop;

uses
  System.StartUpCopy,
  FMX.Forms,
  loginsrc in 'src\loginsrc.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
